# 구글 서비스 계정 인증 설정 (둘 중 하나 사용)
# 1) 서비스 계정 키 JSON 파일 경로
GOOGLE_APPLICATION_CREDENTIALS=service_account.json
# 2) 또는 서비스 계정 키 JSON 내용을 그대로 붙여넣기 (한 줄 JSON 문자열)
SERVICE_ACCOUNT_JSON=

# 대상 스프레드시트 ID (마감 안내용)
SPREADSHEET_ID=

# 월보장 관리 시트 ID
JTWOLAB_SHEET_ID=1zRgtvTZ6SZF-bWiMO8qmnIhhrNVsrDxbIj3HvE8Tv3Y
ILRYU_SHEET_ID=1gInZEFprmb_p43SPKUZT6zjpG36AQPW9OpcsljgNtxM

# 기본 컬럼명 (시트마다 구조가 달라도 이 컬럼들은 존재)
AGENCY_COLUMN=대행사 명
INTERNAL_COLUMN=내부 진행건
REMAINING_DAYS_COLUMN=마감 잔여일
CHECKED_COLUMN=마감 안내 체크
BIZNAME_COLUMN=상호명

# Flask 서버 설정
FLASK_HOST=0.0.0.0
FLASK_PORT=8080
FLASK_DEBUG=false

# 애드로그 순위 크롤링 (필수! 기본값 없음)
ADLOG_ID=your_adlog_id
ADLOG_PASSWORD=your_adlog_password
ADLOG_URL=https://www.adlog.kr/adlog/naver_place_rank_check.php?sca=&sfl=api_memo&stx=%EC%9B%94%EB%B3%B4%EC%9E%A5&page_rows=100

# 순위 스냅샷 원장 설정 (Google Sheets)
# RANK_SHEET_ID: 별도 순위 전용 시트 사용 시 설정 (선택사항)
# 미설정 시 JTWOLAB_SHEET_ID 사용
RANK_SHEET_ID=your_rank_spreadsheet_id
RANK_SNAPSHOT_TAB=rank_snapshots
RANK_LOG_TAB=rank_update_logs
RANK_STORAGE_MODE=sheets  # sheets (기본) 또는 sqlite (레거시) 또는 both

# 크롤러 설정
HEADLESS=true  # false로 설정하면 브라우저 창이 표시됨 (디버그용)
SAVE_SCREENSHOTS=false
SCREENSHOT_PATH=/tmp/rank_crawler_screenshots

# 스케줄러 설정 (Render 중복 실행 방지)
# workers=1 권장. 또는 USE_INTERNAL_SCHEDULER=false 후 외부 cron 사용
USE_INTERNAL_SCHEDULER=true
CRON_TOKEN=your_secure_random_token_for_cron

# Render 배포 설정
AUTO_SYNC_ON_START=true

# 데이터 저장 경로 (선택사항, 기본값: 현재 디렉토리)
# Render 배포 시 Disk 마운트 경로(/var/data)를 사용하면 서버 재시작 시에도 데이터 유지됨
# PRICEBOOK_PATH=/var/data/pricebook.json
# EXTRA_EXPENSES_PATH=/var/data/extra_expenses.json
# CREAM2_ACCOUNTS_PATH=/var/data/cream2_accounts.json

# 인증 설정
# 세션 암호화 키 (운영 환경에서는 반드시 변경!)
SECRET_KEY=your-secret-key-change-me-in-production
# 초기 관리자 계정 (첫 실행 시 자동 생성)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin1234

# Worklog Cache 설정 (Phase 1 MVP)
WORKLOG_CACHE_FILE=/var/data/worklog_cache.json
WORKLOG_CACHE_TTL_HOURS=24

# Training Dataset 설정 (Phase 1 MVP)
TRAINING_ROWS_FILE=/var/data/training_rows.json
RECIPE_STATS_FILE=/var/data/recipe_stats.json
